/** @type {import('next').NextConfig} */
const nextConfig = {
  // React Strict Mode'u etkinleştirir. Geliştirme sırasında potansiyel sorunları belirlemeye yardımcı olur.
  reactStrictMode: true,
};

export default nextConfig;
